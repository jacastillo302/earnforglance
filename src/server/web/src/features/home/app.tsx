import React from 'react';
import BannerPage from './banner'; // Assuming WelcomePage.tsx is in the same directory

const App: React.FC = () => {
  return (
   <BannerPage />
  );
};

export default App;